<Page x:Class="Battleship.Views.BattleView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Battleship.Views"
      mc:Ignorable="d" 
      d:DesignHeight="530" d:DesignWidth="800"
      Title="BattleView">

    <Page.Resources>
        <Style TargetType="Button" x:Key="BaseButton">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="FloralWhite"/>
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Width" Value="150"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>

        <Style TargetType="Button" x:Key="BoardGridButton" BasedOn="{StaticResource BaseButton}">
            <Setter Property="Background" Value="SkyBlue" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Opacity" Value="0.5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="Black" />
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
        </Style>
    </Page.Resources>

    <Grid>
        <Rectangle Fill="White" />
        <Rectangle>
            <Rectangle.Fill>
                <ImageBrush ImageSource="/Assets/sea_bg.png" Opacity="0.8" Stretch="UniformToFill"/>
            </Rectangle.Fill>
        </Rectangle>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                x:Name="BattlePanelTitle"
                FontSize="24" 
                HorizontalAlignment="Center" 
                FontWeight="SemiBold"
                Margin="10"/>

            <Grid Grid.Row="1" ShowGridLines="True" Margin="15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock FontWeight="Bold" x:Name="EnemyBoard" TextAlignment="Center" FontSize="15" Margin="5"/>
                    <UniformGrid Grid.Row="1" x:Name="TrackingGrid" Margin="5"/>

                    <Border Grid.Row="2" Background="#CCFFFFFF" Padding="3" Opacity="0.5"
                        VerticalAlignment="Center" HorizontalAlignment="Center">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock TextAlignment="Center" Text="Enemy Ships" Width="200" Margin="5"/>
                            <WrapPanel Height="Auto" x:Name="EnemyShipsPanel" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <Grid Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock FontWeight="Bold" TextAlignment="Center" Text="Your Board" FontSize="15" Margin="5"/>
                    <UniformGrid Grid.Row="1" x:Name="OwnGrid" Margin="5"/>

                    <Border Grid.Row="2" Background="#CCFFFFFF" Padding="3" Opacity="0.5"
                        VerticalAlignment="Center" HorizontalAlignment="Center">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock x:Name="YourShips" TextAlignment="Center" Width="200" Margin="5"/>
                            <WrapPanel Height="Auto" x:Name="OwnShipsPanel" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Page>
