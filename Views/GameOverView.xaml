<Page x:Class="Battleship.Views.GameOverView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Battleship.Views"
      mc:Ignorable="d" 
      d:DesignHeight="530" d:DesignWidth="800"
      Title="GameOverView">

    <Page.Resources>
        <Style TargetType="Button" x:Key="BaseButton">
            <Setter Property="Background" Value="#333"/>
            <Setter Property="Foreground" Value="FloralWhite"/>
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="15" />
            <Setter Property="Height" Value="30" />
            <Setter Property="Width" Value="150"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#444"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#222"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#666"/>
                                <Setter Property="Foreground" Value="#AAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <Rectangle Fill="White" />
        <Rectangle>
            <Rectangle.Fill>
                <ImageBrush ImageSource="/Assets/sea_bg.png" Opacity="0.8" Stretch="UniformToFill"/>
            </Rectangle.Fill>
        </Rectangle>

        <Border Background="#CCFFFFFF" Margin="90" CornerRadius="10" Opacity="0.7">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Game Over!" FontSize="36" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>
                <TextBlock x:Name="WinnerField" FontSize="24" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <TextBlock Text="Thank you for playing!" FontSize="24" Margin="0,0,0,10" HorizontalAlignment="Center"/>
                <Button Content="Restart Game" Style="{StaticResource BaseButton}" Click="RestartGame_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
